{
  "name": "schema",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm run format && run-s \"build:*\"",
    "build:backend": "tsp compile src/backend",
    "format": "tsp format .",
    "check:updates": "pnpm outdated --recursive --long"
  },
  "devDependencies": {
    "@typespec/compiler": "^1.14.0",
    "@typespec/http": "^1.14.0",
    "@typespec/openapi": "^1.14.0",
    "@typespec/openapi3": "^1.14.0",
    "npm-run-all2": "^9.0.2"
  }
}
